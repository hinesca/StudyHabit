<UserControl x:Class="StudyHabit.View.CourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudyHabit"
             mc:Ignorable="d" 
             d:DesignHeight="350" Width="175">
     <UserControl.Resources>
          <local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
     </UserControl.Resources>
     <DockPanel>
          <TabControl>
               <TabItem Header="Classes">
                    <DockPanel>
                         <Button DockPanel.Dock="Top" Width="50">
                              Add Class
                         </Button>
                         <ListView DataContext="{Binding CourseList}">
                              
                         </ListView>
                         <TextBlock Text="{Binding SelectedCourseName}"/>
                         <StackPanel Orientation="Horizontal">
                              <ListView>
                                   
                              </ListView>
                              <StackPanel>
                                   <TextBlock Text="To Do" FontWeight="Bold"/>
                                   <ListView>
                                        
                                   </ListView>
                              </StackPanel>
                         </StackPanel>
                    </DockPanel>
               </TabItem>
          </TabControl>
     </DockPanel>
</UserControl>
